{
  "version": 2,
  "routes": [
    {
      "src": "/(.*?)",
      "dest": "index.js",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH"],
      "headers": [
        {
          "source": "/(.*)",
          "headers": [
            { "key": "Access-Control-Allow-Origin", "value": "*" }
          ]
        },
        {
          "source": "/vercel_app_domain_name/(.*)",
          "headers": [
            { "key": "Access-Control-Allow-Origin", "value": "*" }
          ]
        }
      ]
    }
  ]
}
